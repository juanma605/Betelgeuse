# Núcleo: configuración y análisis. Con esto solo ya corre `analyze --demo`.
pandas>=2.0
PyYAML>=6.0

# Fuente MercadoLibre: su sitio viene armado del servidor, así que alcanza
# con una request HTTP y parsear el HTML, sin navegador.
httpx>=0.27
beautifulsoup4>=4.12

# Fuentes sin API (Zonaprop, Argenprop, Mudafy, Remax). Además del paquete
# hay que bajar el navegador una vez: `playwright install chromium`.
playwright>=1.40

# Dashboard.
streamlit>=1.30
